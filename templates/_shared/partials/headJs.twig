{# Disable automatic registering and loading of the htmx script #}
{% do sprig.setRegisterScript(false) %}

{# Use a local copy of htmx in production, rather than rely on a CDN #}
<script defer src="https://cdn.jsdelivr.net/gh/bigskysoftware/htmx/src/htmx.min.js"></script>

{# Include our bundle #}
{{ craft.vite.script("src/scripts/main.js", false) }}